import { RestaurantSearchResult } from './restaurant-search-result';

describe('RestaurantSearchResult', () => {
  it('should create an instance', () => {
    expect(new RestaurantSearchResult()).toBeTruthy();
  });
});
