import { RestaurantForCreation } from './restaurant-for-creation';

describe('RestaurantForCreation', () => {
  it('should create an instance', () => {
    expect(new RestaurantForCreation()).toBeTruthy();
  });
});
