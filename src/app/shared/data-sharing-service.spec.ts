import { DataSharingService } from './data-sharing-service';

describe('DataSharingService', () => {
  it('should create an instance', () => {
    expect(new DataSharingService()).toBeTruthy();
  });
});
